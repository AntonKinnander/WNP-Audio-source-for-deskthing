---
phase: 02-project-setup
plan: 03
type: execute
---

<objective>
Set up the build configuration using Vite and deskthing.config.ts for development and packaging.

Purpose: Configure the build system to compile TypeScript, bundle the server code, and integrate with Deskthing's development workflow.

Output: Working vite.config.ts and deskthing.config.ts that enable `npm run dev` and `npm run build`.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@Deskthing-app-examples/audio/vite.config.ts
@Deskthing-app-examples/audio/deskthing.config.ts
@.planning/phases/02-project-setup/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite configuration</name>
  <files>vite.config.ts</files>
  <action>Create vite.config.ts based on audio app example. Configure: 1) Build entry point from server/index.ts, 2) Output format (ES modules or CJS depending on Deskthing requirements), 3) OutDir to dist/, 4) TypeScript plugin, 5) Development server settings (if needed for frontend). Copy the general structure from audio app but adapt for our app name.</action>
  <verify>vite.config.ts is valid TypeScript, `npm run build` succeeds with empty server/index.ts placeholder</verify>
  <done>Vite configured for Deskthing app builds</done>
</task>

<task type="auto">
  <name>Task 2: Create Deskthing configuration</name>
  <files>deskthing.config.ts</files>
  <action>Create deskthing.config.ts following audio app pattern. This configures the Deskthing CLI integration. Include: 1) App ID matching manifest.json, 2) Entry point configuration, 3) Dev/build environment settings. This file tells the Deskthing development server how to run our app.</action>
  <verify>deskthing.config.ts is valid TypeScript, structure matches audio app</verify>
  <done>Deskthing CLI integration configured</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder server entry point and test build</name>
  <files>server/index.ts</files>
  <action>Create a minimal server/index.ts with: 1) Import DeskThing from @deskthing/server, 2) Log "WNP Audio Source starting...", 3) Export empty module for now. Run `npm run build` to verify the entire build pipeline works. Create `.gitignore` for node_modules, dist/, .env files.</action>
  <verify>`npm run build` completes successfully, dist/ folder contains built files, no TypeScript errors</verify>
  <done>Build pipeline verified working, placeholder entry point in place</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] vite.config.ts exists and is valid
- [ ] deskthing.config.ts exists and is valid
- [ ] server/index.ts placeholder created
- [ ] `npm run build` succeeds
- [ ] .gitignore created
</verification>

<success_criteria>
- Build configuration complete and functional
- `npm run dev` and `npm run build` work correctly
- Phase 2 (Project Setup) complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-setup/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Build Configuration Summary

**Set up Vite and Deskthing build configuration with verified working build pipeline.**

## Accomplishments
- Created vite.config.ts for TypeScript compilation
- Created deskthing.config.ts for Deskthing CLI integration
- Verified build pipeline works end-to-end

## Files Created/Modified
- `vite.config.ts` - Build configuration
- `deskthing.config.ts` - Deskthing integration
- `server/index.ts` - Placeholder entry point
- `.gitignore` - Git ignore rules

## Decisions Made
- Used same build configuration pattern as audio app
- ES module output format

## Issues Encountered
None

## Next Step
Phase 2 complete. Ready for Phase 3: WNP Server (03-01-PLAN.md)
</output>
