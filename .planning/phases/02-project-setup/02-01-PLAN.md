---
phase: 02-project-setup
plan: 01
type: execute
---

<objective>
Initialize the Deskthing app project structure with package.json and TypeScript configuration.

Purpose: Set up the foundational project files following Deskthing app conventions, creating a proper Node.js/TypeScript project structure.

Output: Working package.json, tsconfig.json, and folder structure matching Deskthing app examples.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@Deskthing-app-examples/audio/package.json
@Deskthing-app-examples/audio/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project folder structure</name>
  <files>server/, src/, deskthing/, dist/</files>
  <action>Create the standard Deskthing app folder structure: 1) `server/` - Backend TypeScript code (entry point, business logic), 2) `src/` - Frontend UI code (can be minimal placeholder for MVP), 3) `deskthing/` - App metadata and assets, 4) `dist/` - Build output. Create empty README.md explaining this is a Deskthing audio source app for WNP.</action>
  <verify>Directories exist with correct names, README.md present with project description</verify>
  <done>Folder structure matches Deskthing app conventions</done>
</task>

<task type="auto">
  <name>Task 2: Initialize package.json with dependencies</name>
  <files>package.json</files>
  <action>Create package.json following Deskthing app examples. Include: 1) `@deskthing/server` SDK dependency, 2) `@deskthing/types` for TypeScript types, 3) TypeScript and build tooling (vite or esbuild), 4) Scripts: dev, build, lint. Set name to "wnp-audio-source", version to "1.0.0". DO NOT add WNP-specific dependencies yet (Phase 3).</action>
  <verify>package.json is valid JSON, contains @deskthing/server and @deskthing/types, scripts include dev/build/lint</verify>
  <done>package.json exists with Deskthing SDK dependencies and standard scripts</done>
</task>

<task type="auto">
  <name>Task 3: Create TypeScript configuration</name>
  <files>tsconfig.json</files>
  <action>Create tsconfig.json based on audio app example. Configure: 1) Target ES2020 or higher, 2) Module resolution (bundler or node16), 3) OutDir to dist/, 4) SourceRoot to project root, 5) Strict mode enabled. Include paths for @deskthing imports if needed.</action>
  <verify>tsconfig.json is valid JSON, compiles without errors, matches audio app configuration pattern</verify>
  <done>TypeScript configured for Deskthing app development</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Folder structure created (server/, src/, deskthing/, dist/)
- [ ] package.json exists with @deskthing/server dependency
- [ ] tsconfig.json exists and validates
- [ ] npm install completes without errors
</verification>

<success_criteria>
- Project structure matches Deskthing app conventions
- All configuration files are valid
- Dependencies can be installed successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-setup/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Project Initialization Summary

**Initialized Deskthing app project structure with package.json and TypeScript configuration.**

## Accomplishments
- Created Deskthing app folder structure
- Set up package.json with @deskthing/server SDK
- Configured TypeScript for the project

## Files Created/Modified
- `package.json` - Project dependencies and scripts
- `tsconfig.json` - TypeScript configuration
- `server/`, `src/`, `deskthing/`, `dist/` - Folder structure

## Decisions Made
- Used Vite for build tooling (matching audio app)
- Target ES2020 for broad compatibility

## Issues Encountered
None

## Next Step
Ready for 02-02-PLAN.md - Create manifest.json
</output>
