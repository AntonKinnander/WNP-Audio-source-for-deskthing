---
phase: 02-project-setup
plan: 02
type: execute
---

<objective>
Create the Deskthing app manifest.json file with proper metadata and port configuration.

Purpose: The manifest defines the app to Deskthing server - its ID, name, type (audio source), and assigned port 6344 for WNP communication.

Output: Valid manifest.json in deskthing/ directory with correct audio source configuration.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@Deskthing-app-examples/audio/deskthing/manifest.json
@.planning/phases/02-project-setup/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manifest.json with app metadata</name>
  <files>deskthing/manifest.json</files>
  <action>Create manifest.json following Deskthing audio source pattern. Include: 1) `id`: "wnp-audio-source", 2) `label`: "WebNowPlaying Audio Source", 3) `version`: "1.0.0", 4) `description`: "Displays music metadata from web browsers via WNP protocol", 5) `author`: Your name, 6) `tags`: ["audiosource"], 7) `platforms`: ["windows", "linux", "mac"], 8) `requiredVersions`: server >=0.11.13, client >=0.11.2 (matching audio app).</action>
  <verify>manifest.json is valid JSON, all required fields present, tags include "audiosource"</verify>
  <done>Manifest exists with complete app metadata</done>
</task>

<task type="auto">
  <name>Task 2: Add port configuration and icon placeholder</name>
  <files>deskthing/manifest.json, deskthing/icon.png</files>
  <action>Add to manifest.json: 1) `port`: 6344 (assigned WNP adapter port), 2) `portDescription`: "WebNowPlaying adapter port". Add a placeholder icon.png (can copy from audio app or create a simple one - 512x512px). The icon should represent music/browser integration.</action>
  <verify>manifest.json includes port 6344, icon.png exists in deskthing/ folder</verify>
  <done>Port configuration and icon in place</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] deskthing/manifest.json exists and is valid JSON
- [ ] Port 6344 is specified
- [ ] Tags include "audiosource"
- [ ] Icon asset exists
</verification>

<success_criteria>
- Manifest is valid and complete
- Port 6344 is properly configured
- App will be recognized as audio source by Deskthing
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-setup/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Manifest Creation Summary

**Created Deskthing app manifest.json with WNP port 6344 configuration.**

## Accomplishments
- Created complete manifest.json with app metadata
- Configured port 6344 for WNP communication
- Added app icon asset

## Files Created/Modified
- `deskthing/manifest.json` - App definition
- `deskthing/icon.png` - App icon

## Decisions Made
None (following Deskthing conventions)

## Issues Encountered
None

## Next Step
Ready for 02-03-PLAN.md - Set up build configuration
</output>
