---
phase: 03-wnp-server
plan: 02
type: execute
---

<objective>
Implement WNP message receiving and parsing based on the protocol reverse-engineered in Phase 1.

Purpose: The browser extension sends metadata updates as WebSocket messages. We need to receive these messages, parse the JSON, and extract the WNP data fields.

Output: Working message handler that receives WNP messages and extracts metadata into a structured format.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-research/FINDINGS.md
@.planning/phases/03-wnp-server/03-01-SUMMARY.md
@Documentation/WNP-to-Deskthing-Mapping.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define WNP data types and interfaces</name>
  <files>server/types.ts</files>
  <action>Create server/types.ts defining TypeScript interfaces for WNP data: 1) `WNPPlayer` interface with all fields from FINDINGS.md (id, name, title, artist, album, cover_url, state, position, duration, volume, etc.), 2) `WNPMessage` interface for incoming messages (event type, player data), 3) Enums for state (Playing, Paused, Stopped), repeat mode, etc. Use the field names discovered in Phase 1 research.</action>
  <verify>types.ts defines WNPPlayer and WNPMessage interfaces, all fields from FINDINGS.md are represented</verify>
  <done>TypeScript interfaces defined for WNP protocol data structures</done>
</task>

<task type="auto">
  <name>Task 2: Implement message listener and JSON parsing</name>
  <files>server/wnpServer.ts</files>
  <action>Add to WNPServer class: 1) `on('connection')` handler that stores client and sets up message listener, 2) Message listener that parses incoming JSON, 3) Error handling for invalid JSON, 4) Callback/emit pattern for parsed WNP messages. When a valid WNP message is received, emit an event with the parsed WNPPlayer data. Log incoming messages for debugging (can be disabled later).</action>
  <verify>Server receives messages from browser extension, JSON parses successfully, WNPPlayer data is extracted, events are emitted</verify>
  <done>WebSocket messages are received and parsed into WNP data structures</done>
</task>

<task type="auto">
  <name>Task 3: Add player state tracking and active player management</name>
  <files>server/wnpServer.ts</files>
  <action>Extend WNPServer to: 1) Track connected players by ID, 2) Identify "active" player (most recently updated or explicitly marked), 3) Store current state for each player, 4) Provide `getActivePlayer()` method. This handles cases where multiple sources might connect (though browser extension typically connects once). State should update on each message received.</action>
  <verify>Server tracks player state, getActivePlayer() returns current player, state updates on messages</verify>
  <done>Player state management implemented with active player tracking</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>WebSocket message handling that receives and parses WNP protocol messages</what-built>
  <how-to-verify>
    1. Start the Deskthing app with WNP Server
    2. In browser, configure WNP extension to connect to localhost:6344
    3. Play music on a supported site (YouTube, Spotify Web)
    4. Check server logs show connection and parsed metadata
    5. Verify title, artist, album are logged correctly
  </how-to-verify>
  <resume-signal>Type "verified" to continue, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] WNP data types defined in types.ts
- [ ] Message listener parses JSON correctly
- [ ] WNPPlayer data is extracted from messages
- [ ] Player state is tracked
- [ ] Connection with browser extension verified
</verification>

<success_criteria>
- Messages from browser extension are received
- JSON parsing works for all WNP fields
- Player state tracking functional
- Phase 3 (WNP Server) complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-wnp-server/03-02-SUMMARY.md`:

# Phase 3 Plan 2: WNP Message Handling Summary

**Implemented WNP message receiving, parsing, and player state tracking.**

## Accomplishments
- Defined TypeScript interfaces for WNP protocol
- Implemented WebSocket message parsing
- Added player state tracking and active player management
- Verified connection with browser extension

## Files Created/Modified
- `server/types.ts` - WNP data type definitions
- `server/wnpServer.ts` - Message handling and state tracking

## Decisions Made
- Event-based architecture for message propagation
- Active player determined by most recent update

## Issues Encountered
None (or describe any issues with browser extension connection)

## Next Step
Phase 3 complete. Ready for Phase 4: Data Translation (04-01-PLAN.md)
</output>
