---
phase: 03-wnp-server
plan: 01
type: execute
---

<objective>
Implement a WebSocket server on port 6344 that can accept connections from the WNP browser extension.

Purpose: The browser extension connects to adapters via WebSocket. We need to create a server that listens on port 6344, accepts connections, and is ready to receive WNP messages.

Output: Working WebSocket server that starts when the Deskthing app starts and listens on port 6344.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-research/FINDINGS.md
@.planning/phases/02-project-setup/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WebSocket dependency and create server module</name>
  <files>package.json, server/wnpServer.ts</files>
  <action>Add `ws` package to package.json dependencies for WebSocket server support. Create server/wnpServer.ts with: 1) Export a class `WNPServer` that manages the WebSocket connection, 2) Constructor takes port number (6344), 3) Methods: `start()`, `stop()`, `isRunning` property. Use the `ws` library's WebSocketServer class. DO NOT implement message handling yet - just the server lifecycle.</action>
  <verify>package.json includes ws dependency, wnpServer.ts exports WNPServer class with start/stop methods</verify>
  <done>WebSocket server module created with basic lifecycle methods</done>
</task>

<task type="auto">
  <name>Task 2: Implement server start/stop logic with port 6344</name>
  <files>server/wnpServer.ts</files>
  <action>Implement the WNPServer methods: 1) `start()` creates WebSocketServer on port 6344, logs "WNP Server listening on port 6344", handles connection events, 2) `stop()` closes the server and all client connections, 3) Handle server errors (port already in use, etc.) with try-catch. Store connected clients for later message handling. Add connection logging for debugging.</action>
  <verify>Server starts successfully on port 6344, accepts WebSocket connections, logs connections, stops cleanly</verify>
  <done>Server can start/stop and accepts WebSocket connections</done>
</task>

<task type="auto">
  <name>Task 3: Integrate WNP Server with Deskthing app lifecycle</name>
  <files>server/index.ts</files>
  <action>Update server/index.ts to: 1) Import WNPServer and DeskThing from @deskthing/server, 2) Create WNPServer instance, 3) On DESKTHING_EVENTS.START, call `wnpServer.start()`, 4) On DESKTHING_EVENTS.STOP, call `wnpServer.stop()`, 5) Add error handling and logging. This ensures the WNP server runs only when the Deskthing app is active.</action>
  <verify>Server starts when app starts, stops when app stops, no errors in lifecycle transitions</verify>
  <done>WNP Server integrated with Deskthing app lifecycle</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ws dependency added and installed
- [ ] WNPServer class implements start/stop
- [ ] Server listens on port 6344
- [ ] Server starts/stops with Deskthing app
- [ ] Connections are logged
</verification>

<success_criteria>
- WebSocket server running on port 6344
- Server integrates with Deskthing START/STOP events
- Browser extension can connect (verify connection logs)
</success_criteria>

<output>
After completion, create `.planning/phases/03-wnp-server/03-01-SUMMARY.md`:

# Phase 3 Plan 1: WebSocket Server Summary

**Implemented WebSocket server on port 6344 that accepts connections from WNP browser extension.**

## Accomplishments
- Created WNPServer class with WebSocket support
- Server listens on assigned port 6344
- Integrated with Deskthing app lifecycle

## Files Created/Modified
- `server/wnpServer.ts` - WebSocket server implementation
- `server/index.ts` - Updated with WNPServer lifecycle
- `package.json` - Added ws dependency

## Decisions Made
- Used `ws` library for WebSocket server (standard Node.js choice)
- Server managed through Deskthing START/STOP events

## Issues Encountered
None

## Next Step
Ready for 03-02-PLAN.md - Handle WNP message parsing
</output>
