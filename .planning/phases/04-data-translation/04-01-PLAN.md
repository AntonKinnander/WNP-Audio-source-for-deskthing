---
phase: 04-data-translation
plan: 01
type: execute
---

<objective>
Create conversion functions that transform WNP data into Deskthing's SongData11 format.

Purpose: WNP sends data in its own format with different field names and types. We need converter functions that translate this to SongData11, handling time conversions, state mappings, and null values.

Output: Conversion functions that produce valid SongData11 from WNPPlayer data.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@Documentation/WNP-to-Deskthing-Mapping.md
@.planning/phases/03-wnp-server/03-02-SUMMARY.md
@Deskthing-app-examples/audio/server/mediaStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create converter utility module</name>
  <files>server/converter.ts</files>
  <action>Create server/converter.ts with conversion functions: 1) `wnpToSongData11(wnpPlayer: WNPPlayer): SongData11` - main converter, 2) Time conversion helpers (`timeToMillis`, `millisToTime` if needed), 3) State conversion helpers (`wnpStateToIsPlaying`, `wnpRepeatToDeskthing`). Follow the mapping documented in WNP-to-Deskthing-Mapping.md. Handle null/undefined values by using sensible defaults or null.</action>
  <verify>converter.ts exports wnpToSongData11 function, all WNP fields map to SongData11 fields, time conversions correct, state mappings correct</verify>
  <done>Converter module produces valid SongData11 from WNP data</done>
</task>

<task type="auto">
  <name>Task 2: Handle time format conversions</name>
  <files>server/converter.ts</files>
  <action>Implement time conversion logic in converter.ts: 1) Parse WNP time formats (HH:MM:SS, seconds, milliseconds) into milliseconds for SongData11, 2) Handle edge cases (empty strings, negative values), 3) Return null for invalid/missing values. WNP may send time in various formats - handle all cases found in Phase 1 research.</action>
  <verify>Time conversions work for all WNP formats (HH:MM:SS → ms, seconds → ms, ms → ms), invalid times return null</verify>
  <done>Time conversions handle all WNP formats correctly</done>
</task>

<task type="auto">
  <name>Task 3: Map capability flags and abilities</name>
  <files>server/converter.ts</files>
  <action>Add ability mapping to converter.ts: 1) Convert WNP boolean flags (can_set_state, can_skip_next, etc.) to SongAbilities array, 2) Map repeat mode enum (NONE/ALL/ONE → "off"/"all"/"track"), 3) Map shuffle boolean, 4) Build abilities array based on available flags. Reference the mapping table in WNP-to-Deskthing-Mapping.md.</action>
  <verify>WNP capability flags map to SongAbilities, repeat modes convert correctly, shuffle state converts correctly</verify>
  <done>Capability flags and abilities properly mapped</done>
</task>

<task type="auto">
  <name>Task 4: Handle album art and thumbnails</name>
  <files>server/converter.ts</files>
  <action>Implement album art handling: 1) Extract cover_url from WNP data, 2) Pass URL directly to SongData11.thumbnail (let Deskthing UI handle loading), 3) For MVP, do NOT download/cache images (defer to v2 if needed), 4) Handle missing/invalid URLs (return null). This keeps MVP simple and lets the UI handle image loading.</action>
  <verify>cover_url maps to thumbnail field, null returned for missing URLs, no image downloading in converter</verify>
  <done>Album art URLs mapped correctly without unnecessary complexity</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] converter.ts creates valid SongData11
- [ ] All WNP fields map correctly
- [ ] Time conversions accurate
- [ ] State/ability mappings correct
- [ ] Null handling works
</verification>

<success_criteria>
- Converter produces SongData11 matching Deskthing format
- All field mappings documented in WNP-to-Deskthing-Mapping.md are implemented
- Edge cases (null, invalid data) handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-translation/04-01-SUMMARY.md`:

# Phase 4 Plan 1: Data Conversion Summary

**Created conversion functions transforming WNP data to SongData11 format.**

## Accomplishments
- Implemented wnpToSongData11 converter
- Added time format conversions
- Mapped capability flags to SongAbilities
- Handled album art URL mapping

## Files Created/Modified
- `server/converter.ts` - WNP to SongData11 conversion functions

## Decisions Made
- Pass cover URLs directly (no caching in MVP)
- Return null for missing/invalid data
- Handle multiple WNP time formats

## Issues Encountered
None

## Next Step
Ready for 04-02-PLAN.md - Integrate with Deskthing SDK
</output>
