---
phase: 04-data-translation
plan: 02
type: execute
---

<objective>
Integrate the WNP server and converter with Deskthing SDK to send SongData11 updates to the Deskthing interface.

Purpose: Connect all the pieces - when WNP sends metadata, we convert it and notify Deskthing via the SDK's sendSong method. This makes the music display work.

Output: Working end-to-end flow from browser extension to Deskthing UI display.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/04-data-translation/04-01-SUMMARY.md
@Deskthing-app-examples/audio/server/mediaStore.ts
@Deskthing-app-examples/audio/server/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MediaStore for WNP data management</name>
  <files>server/mediaStore.ts</files>
  <action>Create server/mediaStore.ts following audio app pattern: 1) Singleton class managing current WNP player state, 2) Methods: `initializeListeners()` sets up WNP server message handler, `handleGetSong()` sends current state via DeskThing.sendSong(), `handleRefresh()` re-sends current data, 3) Store current WNPPlayer data internally. DO NOT implement control handlers (play/pause/etc.) - those are v2. Focus on GET/REFRESH for metadata display.</action>
  <verify>mediaStore.ts exports singleton getInstance(), has initializeListeners and handleGetSong methods, follows audio app pattern</verify>
  <done>MediaStore singleton created for WNP state management</done>
</task>

<task type="auto">
  <name>Task 2: Wire WNP Server messages to MediaStore</name>
  <files>server/mediaStore.ts, server/wnpServer.ts</files>
  <action>Connect the components: 1) In MediaStore.initializeListeners(), subscribe to WNPServer message events, 2) On WNP message, convert using wnpToSongData11(), 3) Store converted data, 4) Call DeskThing.sendSong() to push update to Deskthing, 5) Log update for debugging. This creates the flow: Browser → WNP Server → Converter → Deskthing SDK → UI.</action>
  <verify>WNP messages trigger SongData11 updates, DeskThing.sendSong() is called with valid data, UI shows metadata</verify>
  <done>End-to-end data flow working from browser to Deskthing</done>
</task>

<task type="auto">
  <name>Task 3: Set up Deskthing SDK event listeners</name>
  <files>server/index.ts</files>
  <action>Update server/index.ts to: 1) Import MediaStore and SongEvent from @deskthing/types, 2) On START event, initialize MediaStore listeners, 3) On SongEvent.GET, call mediaStore.handleGetSong(), 4) On SongEvent.REFRESH, call mediaStore.handleRefresh(), 5) Log all SDK events for debugging. This makes the app respond to Deskthing's requests for song data.</action>
  <verify>SDK events are logged, GET requests return current song data, REFRESH re-sends data</verify>
  <done>Deskthing SDK event listeners configured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete integration from browser extension to Deskthing UI</what-built>
  <how-to-verify>
    1. Start Deskthing server
    2. Run `npm run dev` for our app
    3. In browser, play music on YouTube/Spotify with WNP extension connected
    4. In Deskthing UI, verify: title displays, artist displays, album displays, cover art shows, progress updates, playing state indicator works
    5. Test REFRESH command in Deskthing UI
  </how-to-verify>
  <resume-signal>Type "verified" to continue, or describe what's not working</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] MediaStore follows audio app pattern
- [ ] WNP messages convert to SongData11
- [ ] DeskThing.sendSong() called on updates
- [ ] SDK event listeners respond to GET/REFRESH
- [ ] UI displays metadata correctly
</verification>

<success_criteria>
- End-to-end flow working
- All MVP metadata displays in Deskthing UI
- Phase 4 (Data Translation) complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-translation/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Deskthing Integration Summary

**Integrated WNP server and converter with Deskthing SDK for end-to-end metadata display.**

## Accomplishments
- Created MediaStore singleton for WNP state
- Connected WNP messages to Deskthing SDK
- Implemented SDK event listeners (GET/REFRESH)
- Verified end-to-end flow to UI

## Files Created/Modified
- `server/mediaStore.ts` - WNP state management
- `server/wnpServer.ts` - Event emission for messages
- `server/index.ts` - SDK event listeners

## Decisions Made
- Followed audio app MediaStore pattern
- Control handlers deferred to v2 (YAGNI)
- Direct URL passing for album art

## Issues Encountered
None (or describe any UI issues found)

## Next Step
Phase 4 complete. Ready for Phase 5: Testing (05-01-PLAN.md)
</output>
