---
phase: 06-basic-controls
plan: 02
type: execute
---

<objective>
Verify WNP server command sending and test bidirectional control flow.

Purpose: Ensure control commands from Deskthing flow through to the browser extension via WNP protocol.
Output: Working bidirectional controls with verification checkpoint.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/06-basic-controls/06-01-SUMMARY.md
@server/wnpServer.ts
@server/mediaStore.ts
@Documentation/WNP-Protocol-Findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WNP server sendCommand implementation</name>
  <files>server/wnpServer.ts</files>
  <action>Review and verify the sendCommand method in WNPServer:
  1. Confirm it sends JSON messages to connected clients
  2. Verify command format: {command: "play", id: playerID}
  3. Ensure it handles all control commands: play, pause, skip-next, skip-previous
  4. Add logging to confirm commands are sent (log payload before client.send)

  The sendCommand method should already exist from Phase 3 - just verify and enhance logging.</action>
  <verify>sendCommand logs outgoing commands, all commands supported</verify>
  <done>WNP server ready to send control commands</done>
</task>

<task type="auto">
  <name>Task 2: Add command confirmation logging</name>
  <files>server/mediaStore.ts</files>
  <action>Enhance control handlers with detailed logging:
  1. Log when command is received from Deskthing
  2. Log WNP command being sent
  3. Use clear prefixes: "Control: PLAY received", "WNP: Sending play command"

  This helps verify the full flow: Deskthing → SDK → MediaStore → WNP Server → Browser</action>
  <verify>Each control step is logged, flow is traceable</verify>
  <done>Full command flow is logged for debugging</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete bidirectional control flow from Deskthing to browser</what-built>
  <how-to-verify>
    1. Build and run app in Deskthing
    2. Play music in browser (YouTube/Spotify)
    3. In Deskthing UI:
       - Click Play/Pause - verify music plays/pauses
       - Click Next - verify track skips
       - Click Previous - verify goes back
    4. Check logs for full command flow
    5. Test all 5 controls work correctly
  </how-to-verify>
  <resume-signal>Type "controls working" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] sendCommand verified working
- [ ] Full command flow logged
- [ ] All controls tested in browser
- [ ] Phase 6 complete
</verification>

<success_criteria>
- Play/Pause works from Deskthing
- Skip Next/Previous works
- Logs show full command flow
- Browser extension receives and executes commands
</success_criteria>

<output>
After completion, create `.planning/phases/06-basic-controls/06-02-SUMMARY.md`:

# Phase 6 Plan 2: Control Verification Summary

**Verified and tested bidirectional controls from Deskthing to browser.**

## Accomplishments
- Verified WNP server command sending
- Added comprehensive logging
- Tested all 5 basic controls

## Files Created/Modified
- `server/wnpServer.ts` - Enhanced logging
- `server/mediaStore.ts` - Command logging

## Test Results
- Play/Pause: [Pass/Fail]
- Next: [Pass/Fail]
- Previous: [Pass/Fail]
- Stop: [Pass/Fail]

## Next Step
Phase 6 complete. Ready for Phase 7: Shuffle & Loop
</output>
