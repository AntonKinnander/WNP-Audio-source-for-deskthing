---
phase: 06-basic-controls
plan: 01
type: execute
---

<objective>
Implement control handler methods in MediaStore for Play/Pause/Skip commands.

Purpose: Deskthing sends control commands via SongEvent.SET that we need to receive and translate to WNP protocol commands.
Output: MediaStore methods for handling basic playback controls.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/05-testing/05-02-SUMMARY.md
@server/mediaStore.ts
@server/wnpServer.ts
@server/types.ts
@Deskthing-app-examples/audio/server/mediaStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add control handler methods to MediaStore</name>
  <files>server/mediaStore.ts</files>
  <action>Add control handler methods to MediaStore class following the audio app pattern:
  1. `handlePlay()` - Calls wnpServer.sendCommand('play')
  2. `handlePause()` - Calls wnpServer.sendCommand('pause')
  3. `handleNext()` - Calls wnpServer.skipNext()
  4. `handlePrevious()` - Calls wnpServer.skipPrevious()
  5. `handleStop()` - Calls wnpServer.sendCommand('stop')

  Each method should log the action for debugging. Reference audio app's pattern for consistency.</action>
  <verify>Methods exist in MediaStore, each calls appropriate wnpServer method with logging</verify>
  <done>All 5 control handler methods implemented</done>
</task>

<task type="auto">
  <name>Task 2: Wire handlers to SongEvent.SET</name>
  <files>server/index.ts</files>
  <action>Update the SongEvent.SET listener in index.ts to route commands to MediaStore:
  1. Import AUDIO_REQUESTS enum for command types
  2. Map PLAY → mediaStore.handlePlay()
  3. Map PAUSE → mediaStore.handlePause()
  4. Map NEXT → mediaStore.handleNext()
  5. Map PREVIOUS → mediaStore.handlePrevious()
  6. Map STOP → mediaStore.handleStop()
  7. Log each received command for debugging

  Follow the switch case pattern from audio app.</action>
  <verify>SET listener routes all 5 commands correctly, TypeScript compiles</verify>
  <done>All basic controls wired to SDK events</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compilation passes
- [ ] All control methods implemented
- [ ] SET event listener routes all commands
- [ ] Logging added for debugging
</verification>

<success_criteria>
- Control handlers follow audio app pattern
- All basic playback commands route correctly
- Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-basic-controls/06-01-SUMMARY.md`:

# Phase 6 Plan 1: Control Handlers Summary

**Implemented MediaStore control handler methods for basic playback.**

## Accomplishments
- Added handlePlay, handlePause, handleNext, handlePrevious, handleStop methods
- Wired handlers to SongEvent.SET in index.ts
- Added command logging for debugging

## Files Created/Modified
- `server/mediaStore.ts` - Control handler methods
- `server/index.ts` - SET event routing

## Next Step
Ready for 06-02-PLAN.md - Wire to WNP server and verify
</output>
