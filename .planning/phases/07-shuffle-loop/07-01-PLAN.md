---
phase: 07-shuffle-loop
plan: 01
type: execute
---

<objective>
Implement shuffle toggle control from Deskthing to WNP.

Purpose: Allow users to toggle shuffle mode from the Deskthing UI.
Output: Working shuffle toggle handler.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/06-basic-controls/06-02-SUMMARY.md
@server/mediaStore.ts
@server/wnpServer.ts
@server/converter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shuffle handler to MediaStore</name>
  <files>server/mediaStore.ts</files>
  <action>Add handleShuffle method to MediaStore:
  1. Accepts data: {shuffle: boolean}
  2. Calls wnpServer.sendCommand('toggle-shuffle') or equivalent
  3. WNP protocol uses SHUFFLE:0/1 or toggle command
  4. Log the action: "Control: Shuffle set to {true/false}"

  Check WNP findings for exact shuffle command format.</action>
  <verify>handleShuffle method exists, sends correct WNP command</verify>
  <done>Shuffle handler implemented</done>
</task>

<task type="auto">
  <name>Task 2: Wire shuffle to SET event</name>
  <files>server/index.ts</files>
  <action>Add SHUFFLE case to SongEvent.SET listener:
  1. Import AUDIO_REQUESTS.SHUFFLE
  2. Map to mediaStore.handleShuffle(data.shuffle)
  3. Log received shuffle command

  Follow existing switch case pattern.</action>
  <verify>SHUFFLE case added, compiles without error</verify>
  <done>Shuffle wired to SDK events</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] handleShuffle implemented
- [ ] SET event includes SHUFFLE case
- [ ] TypeScript compiles
</verification>

<success_criteria>
- Shuffle control can be triggered from Deskthing
- WNP shuffle command is sent correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-shuffle-loop/07-01-SUMMARY.md`:

# Phase 7 Plan 1: Shuffle Control Summary

**Implemented shuffle toggle from Deskthing to WNP.**

## Accomplishments
- Added handleShuffle method
- Wired to SET event listener

## Files Created/Modified
- `server/mediaStore.ts` - handleShuffle method
- `server/index.ts` - SHUFFLE event routing

## Next Step
Ready for 07-02-PLAN.md - Loop (repeat) control
</output>
