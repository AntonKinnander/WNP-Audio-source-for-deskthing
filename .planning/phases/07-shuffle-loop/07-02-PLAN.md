---
phase: 07-shuffle-loop
plan: 02
type: execute
---

<objective>
Implement repeat (loop) toggle control with 3-state support.

Purpose: Handle WNP's 3 repeat modes (off/all/one) and map to Deskthing's repeat state.
Note: Deskthing may only support on/off - will map "one" to "all" if needed.

Output: Working repeat toggle with state cycling.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/07-shuffle-loop/07-01-SUMMARY.md
@server/mediaStore.ts
@server/wnpServer.ts
@server/converter.ts
@Documentation/WNP-Protocol-Findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeat handler with state cycling</name>
  <files>server/mediaStore.ts</files>
  <action>Add handleRepeat method to MediaStore:
  1. Store current repeat state (from last WNP update)
  2. On REPEAT command: cycle through states (off → all → one → off)
  3. Send WNP repeat command with new state: REPEAT:0/1/2
  4. Or use toggle-repeat command if WNP supports it
  5. Log state transitions

  WNP repeat values: 0=off, 1=one, 2=all

  If Deskthing only sends boolean, map: false→0, true→2 (all)</action>
  <verify>handleRepeat cycles through 3 states, sends correct WNP values</verify>
  <done>Repeat handler with 3-state cycling</done>
</task>

<task type="auto">
  <name>Task 2: Wire repeat to SET event</name>
  <files>server/index.ts</files>
  <action>Add REPEAT case to SongEvent.SET listener:
  1. Import AUDIO_REQUESTS.REPEAT
  2. Map to mediaStore.handleRepeat()
  3. Handle both boolean and string repeat states from Deskthing
  4. Log received repeat command</action>
  <verify>REPEAT case added, handles Deskthing repeat format</verify>
  <done>Repeat wired to SDK events</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Shuffle and repeat controls from Deskthing</what-built>
  <how-to-verify>
    1. Build and run app in Deskthing
    2. Play music in browser
    3. Test Shuffle toggle:
       - Click shuffle in Deskthing
       - Verify shuffle state changes in browser
    4. Test Repeat toggle:
       - Click repeat in Deskthing
       - Verify repeat cycles: off → all → one → off
       - Check if Deskthing shows different icons for all vs one
  </how-to-verify>
  <resume-signal>Type "shuffle/loop working" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] handleRepeat cycles through 3 states
- [ ] REPEAT event wired
- [ ] Shuffle and repeat tested
- [ ] Phase 7 complete
</verification>

<success_criteria>
- Shuffle toggle works
- Repeat cycles through off/all/one
- State syncs back to Deskthing
</success_criteria>

<output>
After completion, create `.planning/phases/07-shuffle-loop/07-02-SUMMARY.md`:

# Phase 7 Plan 2: Loop Control Summary

**Implemented repeat (loop) toggle with 3-state support.**

## Accomplishments
- Added handleRepeat with state cycling
- Wired to SET event listener
- Tested shuffle and repeat

## Files Created/Modified
- `server/mediaStore.ts` - handleRepeat method
- `server/index.ts` - REPEAT event routing

## Test Results
- Shuffle: [Pass/Fail]
- Repeat off→all: [Pass/Fail]
- Repeat all→one: [Pass/Fail]
- Repeat one→off: [Pass/Fail]

## Notes
- Deskthing repeat display: [on/off or 3-state?]

## Next Step
Phase 7 complete. Ready for Phase 8: Volume
</output>
