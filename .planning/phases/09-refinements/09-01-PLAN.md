---
phase: 09-refinements
plan: 01
type: execute
---

<objective>
Detect shuffle state changes from the browser to toggle shuffle button in Deskthing.

Purpose: When user toggles shuffle in the browser, detect this and update the shuffle state in Deskthing UI.
Output: Shuffle state detection and sync.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/08-volume/08-02-SUMMARY.md
@server/mediaStore.ts
@server/converter.ts
@server/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track shuffle state changes</name>
  <files>server/mediaStore.ts</files>
  <action>Add shuffle state tracking in MediaStore:
  1. Store lastShuffleState from WNP updates
  2. In handlePlayerUpdate, detect when shuffle_active changes
  3. When shuffle changes, send updated SongData11 with new shuffle_state
  4. Log: "Shuffle state changed: {old} → {new}"

  This ensures shuffle toggles in browser are reflected in Deskthing UI.</action>
  <verify>Shuffle changes are detected and trigger SongData11 updates</verify>
  <done>Shuffle state tracked and synced</done>
</task>

<task type="auto">
  <name>Task 2: Ensure shuffle_state in SongData11</name>
  <files>server/converter.ts</files>
  <action>Verify wnpToSongData11 correctly maps shuffle:
  1. player.shuffle_active (boolean/number) → songData.shuffle_state (boolean/null)
  2. Use wnpShuffleToBoolean helper
  3. Ensure null is handled correctly

  This should already exist from Phase 4 - just verify.</action>
  <verify>shuffle_state field populated correctly in all SongData11</verify>
  <done>Shuffle state conversion verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Shuffle state changes detected
- [ ] shuffle_state field populated
- [ ] Changes trigger Deskthing updates
</verification>

<success_criteria>
- Shuffle toggled in browser reflects in Deskthing UI
- Shuffle button state stays in sync
</success_criteria>

<output>
After completion, create `.planning/phases/09-refinements/09-01-SUMMARY.md`:

# Phase 9 Plan 1: Shuffle Detection Summary

**Implemented shuffle state change detection from browser.**

## Accomplishments
- Added shuffle state tracking
- Verified shuffle_state conversion
- Shuffle changes sync to Deskthing UI

## Files Created/Modified
- `server/mediaStore.ts` - Shuffle state tracking
- `server/converter.ts` - Verified shuffle conversion

## Next Step
Ready for 09-02-PLAN.md - Final polish and testing
</output>
