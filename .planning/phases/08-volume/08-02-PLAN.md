---
phase: 08-volume
plan: 02
type: execute
---

<objective>
Implement volume control from Deskthing to WNP.

Purpose: Allow users to adjust volume from the Deskthing UI.
Output: Working volume slider control.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/08-volume/08-01-SUMMARY.md
@server/mediaStore.ts
@server/wnpServer.ts
@Deskthing-app-examples/audio/server/mediaStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add volume handler to MediaStore</name>
  <files>server/mediaStore.ts</files>
  <action>Add handleVolume method following audio app pattern:
  1. Accepts data: {volume: number} (0-100)
  2. Validate/clamp to 0-100 range
  3. Call wnpServer.setVolume(volume) or sendCommand('set-volume', {volume})
  4. Optional: Debounce rapid volume changes (100-200ms)
  5. Log: "Control: Volume set to X%"

  Reference audio app's volume handling for debouncing pattern.</action>
  <verify>handleVolume validates, clamps, sends to WNP server</verify>
  <done>Volume handler implemented with debouncing</done>
</task>

<task type="auto">
  <name>Task 2: Wire volume to SET event</name>
  <files>server/index.ts</files>
  <action>Add VOLUME case to SongEvent.SET listener:
  1. Import AUDIO_REQUESTS.VOLUME
  2. Map to mediaStore.handleVolume(data.volume)
  3. Log received volume command

  Follow existing pattern.</action>
  <verify>VOLUME case added, compiles without error</verify>
  <done>Volume wired to SDK events</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete volume control from Deskthing to browser</what-built>
  <how-to-verify>
    1. Build and run app in Deskthing
    2. Play music in browser
    3. Test volume sync:
       - Change volume in browser - verify Deskthing updates
    4. Test volume control:
       - Move volume slider in Deskthing
       - Verify browser volume changes
    5. Test volume range:
       - Set to 0 (mute)
       - Set to 100 (max)
       - Set to mid values
  </how-to-verify>
  <resume-signal>Type "volume working" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] handleVolume implemented with debouncing
- [ ] VOLUME event wired
- [ ] Volume control tested
- [ ] Phase 8 complete
</verification>

<success_criteria>
- Volume slider in Deskthing controls browser volume
- Volume changes in browser reflect in Deskthing
- Full 0-100 range works
</success_criteria>

<output>
After completion, create `.planning/phases/08-volume/08-02-SUMMARY.md`:

# Phase 8 Plan 2: Volume Control Summary

**Implemented volume control from Deskthing to WNP.**

## Accomplishments
- Added handleVolume with debouncing
- Wired to SET event listener
- Full bidirectional volume working

## Files Created/Modified
- `server/mediaStore.ts` - handleVolume method
- `server/index.ts` - VOLUME event routing

## Test Results
- Volume sync (browser→Deskthing): [Pass/Fail]
- Volume control (Deskthing→browser): [Pass/Fail]
- Volume range 0-100: [Pass/Fail]

## Next Step
Phase 8 complete. Ready for Phase 9: Refinements
</output>
