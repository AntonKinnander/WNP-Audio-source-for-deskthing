---
phase: 08-volume
plan: 01
type: execute
---

<objective>
Track and sync WNP player volume to Deskthing for display.

Purpose: Show current player volume in Deskthing UI and keep it in sync.
Output: Volume tracking in converter and mediaStore.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/07-shuffle-loop/07-02-SUMMARY.md
@server/converter.ts
@server/mediaStore.ts
@server/wnpServer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure volume is included in SongData11</name>
  <files>server/converter.ts</files>
  <action>Verify and ensure volume field is correctly converted:
  1. wnpToSongData11 should map player.volume to songData.volume
  2. Use clampVolume() to ensure 0-100 range
  3. Handle null/undefined by defaulting to 50
  4. Volume should be included in every SongData11 sent to Deskthing

  This should already exist from Phase 4 - just verify it's working.</action>
  <verify>Volume field is populated in SongData11, logged in output</verify>
  <done>Volume syncs from WNP to Deskthing</done>
</task>

<task type="auto">
  <name>Task 2: Add volume change detection logging</name>
  <files>server/wnpServer.ts</files>
  <action>Enhance VOLUME field processing in processWNPField:
  1. Log when volume changes from WNP: "WNP: Volume changed to X%"
  2. Ensure volume updates trigger playerUpdated event
  3. This ensures volume changes in browser sync to Deskthing UI</action>
  <verify>Volume changes are logged and trigger SongData11 updates</verify>
  <done>Volume changes tracked and synced</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Volume field in SongData11
- [ ] Volume changes logged
- [ ] Volume updates trigger Deskthing sync
</verification>

<success_criteria>
- Volume displays in Deskthing UI
- Volume changes in browser reflect in Deskthing
</success_criteria>

<output>
After completion, create `.planning/phases/08-volume/08-01-SUMMARY.md`:

# Phase 8 Plan 1: Volume Sync Summary

**Enabled volume tracking from WNP to Deskthing.**

## Accomplishments
- Verified volume in SongData11
- Added volume change logging
- Volume syncs from browser to Deskthing

## Files Created/Modified
- `server/converter.ts` - Volume mapping verified
- `server/wnpServer.ts` - Volume change logging

## Next Step
Ready for 08-02-PLAN.md - Volume control
</output>
